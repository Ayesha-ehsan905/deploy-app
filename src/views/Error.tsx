import { Text } from "../components/Text";
import { Box } from "../components/Box";
import React from "react";
const Error = () => {
  return (
    <Box style={{ textAlign: "center", color: "white", fontSize: "40px" }}>
      <Text> Page Not Found</Text>
    </Box>
  );
};

export default Error;
